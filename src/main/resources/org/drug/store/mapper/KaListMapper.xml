<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.drug.store.mapper.KaListMapper">
	<select id="selectAll" resultType="org.drug.store.kalist.KaList">
		SELECT
		 ka_no as kaNo,
		 ka_name as kaName
		 FROM
		 drug_user.ka_master km
		 WHERE
		 del_flg = 0
	</select>

	<insert id="insert_ka_master" parameterType="org.drug.store.karegister.KaRegister">
		INSERT INTO
			drug_user.ka_master(
				ka_no,
				ka_name,
				del_flg,
				register_user_id,
				register_date
			)
		VALUES
			(
				nextval('ka_no_seq'),
				#{rec.kaName},
				0,
				'test1',
				CURRENT_DATE
			)
	</insert>
</mapper>